// Input a string and check whether its first character is uppercase or lowercase. Also convert it to lowercase if the alphabet is uppercase and vice versa. Display the final string
#include <stdio.h>
#include <conio.h>
int main()
{
	
}
